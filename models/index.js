const Documento = require('./document.model');
const Role = require('./role.model');
const Server = require('./server.model');
const Usuario = require('./usuario.model');
const Verificados = require('./verificados.model');

module.exports = {
    Documento,
    Role,
    Server,
    Usuario,
    Verificados
}

